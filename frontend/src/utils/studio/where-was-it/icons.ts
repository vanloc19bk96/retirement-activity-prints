import { PHOSPHOR_DUOTONE_ICON_NAMES } from '../studio-phosphor-icon'

/**
 * Phosphor duotone pool for Where Was It? — same catalog family as Symbol Hunt.
 */
export const WHERE_WAS_IT_ICONS: readonly string[] = PHOSPHOR_DUOTONE_ICON_NAMES
